# Create Branch

Please create a branch using your GitLab username, then push it up:

```bash
git switch -c <gitlab_username>
```

Examples:

```bash
git switch -c tsmith
```

Push:

```bash
git push -u origin tsmith
```

---