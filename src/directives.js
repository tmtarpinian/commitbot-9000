/**
 * WORKSHOP RULES
 *
 * 1. Only add new entries.
 * 2. Never modify existing entries.
 * 3. Always append at the end.
 * 4. One entry per mission step.
 */
export const directives = [
  "Human approval is required.",
  "Clean history before merging.",
  "Resolve conflicts deliberately."
];